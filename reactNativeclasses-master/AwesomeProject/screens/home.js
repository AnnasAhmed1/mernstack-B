import React from 'react';
import AppBar from '../components/app_bar';
import {Text} from 'react-native';

const HomeScreen = () => {
  return <Text>Home</Text>;
};

export default Home = () => {
  return <AppBar Screen={HomeScreen} />;
};
