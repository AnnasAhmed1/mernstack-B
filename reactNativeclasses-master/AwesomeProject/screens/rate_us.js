import React from 'react';
import AppBar from '../components/app_bar';
import {Text} from 'react-native';

const RateUsScreen = () => {
  return <Text>RateUs</Text>;
};

export default RateUs = () => {
  return <AppBar Screen={RateUsScreen} />;
};
