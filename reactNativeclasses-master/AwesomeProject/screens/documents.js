import React from 'react';
import AppBar from '../components/app_bar';
import {Text} from 'react-native';

const DocumentsScreen = () => {
  return <Text>Documents</Text>;
};

export default Documents = () => {
  return <AppBar Screen={DocumentsScreen} />;
};
